export const celsiusToFahrenheit = (celsius: number): number =>
	(celsius * 9) / 5 + 32;

export const bytesToGigabytes = (bytes: number): number => bytes / 1e9;
export const bytesToTerabytes = (bytes: number): number => bytes / 1e12;
